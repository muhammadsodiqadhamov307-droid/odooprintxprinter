# requirements.txt
# Install with: pip install -r requirements.txt
python-escpos==3.1
# xmlrpc is built in to Python 3; listed in the spec for documentation only.
pyusb==1.2.1
Pillow==10.3.0
pywebview==6.0
# Optional - only needed if running as a Windows Service:
# pywin32==306
